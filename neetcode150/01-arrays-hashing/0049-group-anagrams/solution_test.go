package solution

import "testing"

func TestSolve(t *testing.T) {
	tests := []struct {
		name string
		// TODO: 入力と期待値のフィールドを足す
	}{
		{name: "example 1"},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Skip("TODO: テストを書く")
		})
	}
}
